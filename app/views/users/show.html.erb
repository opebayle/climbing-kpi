<p id="notice"><%= notice %></p>

<p>
  <strong>Login:</strong>
  <%= @user.login %>
</p>

<p>
  <strong>Role:</strong>
  <%= @user.role %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>Liste des voies réalisées:</p>
<table>
  <tbody>
    <% @user.routes.each do |route| %>
      <tr>
        <td><%= link_to route.name, route %></td>
        <td><%= Level.find(route.level_id).name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
